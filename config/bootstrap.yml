spring:
  cloud:
    consul:
      host: 127.0.0.1
      #host: 192.168.199.79
      port: 8500
      config:
        enabled: true
        format: FILES
        watch:
          enabled: true
        #prefix: config
        #data-key: LogisticConfig
        prefix: detalase-config/master/config
      discovery:
        healthCheckInterval: 15s
        instance-id: ${spring.application.name}
        service-name: ${spring.application.name}
        enabled: true
